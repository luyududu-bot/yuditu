# 屿地图｜项目提交说明

## 作品简介

屿地图是一款面向中央美术学院花家地校区的视觉探索图鉴。它不是以效率为唯一目标的传统地图，而是让用户通过“清扫尘沙、显影校园”的方式建立空间认知，在发现建筑、展览与服务设施的同时，留下可以被珍藏的校园记忆。

产品进一步将校园地点转化为 AI 可理解的结构化对象。用户不必只依靠按钮和固定路线，而可以表达“我只有 20 分钟，想看最有代表性的地方”等目标。系统会读取地点的类型、标签、关系、可执行操作与时间约束，生成一条可解释的探索路线。

体验以四片校园区域的局部显影为主线。每完成一片区域，该区域会以羽化边缘恢复彩色，地图自动聚焦放大，让用户观察建筑编号、设施图标与局部细节；用户也可以拖动、缩放地图继续探索。发现的地点可以加入 DIY 手账，与自定义文字、贴纸和用户照片自由排版，最终导出为可保存、可分享的校园记忆图片。

## 美院团队的核心竞争力

屿地图的优势不是简单地给通用地图增加一层插画，而是将视觉叙事、空间感知和产品结构同时设计：

1. **以视觉建立空间情感。** 高审美校园地图承担情绪价值与文化表达，让用户愿意探索、记录和分享。
2. **以图鉴组织校园知识。** 建筑、展览、设施与地标不再是零散信息，而是可持续维护的文化对象。
3. **以交互创造记忆。** 清扫显影让“发现地点”成为具备参与感的体验。
4. **以结构连接 AI。** 地点对象拥有机器可理解的语义，使未来 Agent 能够围绕真实空间进行规划和行动。

## AI Native 最小改造

- 统一地图对象模型，为地点补充 `type`、`category`、`tags`、`relations`、`actions`、`constraints` 与 `metadata`。
- 增加轻量目标输入，不改变地图主界面与核心视觉体验。
- 使用本地目标解析与路线推荐证明 goal-based 交互，不依赖现场网络和模型 API。
- 保留标准领域 action，为未来 Agent 接入准备稳定接口。

## Agent 可调用能力

```text
list_objects(filters)
get_object(object_id)
get_available_actions(object_id)
resolve_goal(user_input)
recommend_places(goal)
plan_route(goal, constraints)
focus_on_object(object_id)
open_detail(object_id)
create_journal(object_id, content)
```

Agent 读取结构化对象并调用受控 action，不直接修改底层地图数据。涉及保存或覆盖用户内容时，需要用户确认。

## 商业闭环

### 价值链

学校、展览主办方和文化园区购买定制化视觉导览服务；访客免费使用并通过探索、分享与手账形成传播。结构化对象系统降低内容更新和复制成本，使产品可以扩展至其他艺术院校、博物馆、艺术节与文化园区。

### 收入来源

- 校园与展览定制导览项目服务费
- 长期内容维护与活动运营服务
- 限定地图、贴纸、手账等联名文创
- 品牌合作路线与文化活动联动

### 增长闭环

视觉探索吸引使用 → 路线与地点故事提高停留 → 手账和分享带来传播 → 更多学校与文化空间采购 → 对象与内容模板持续复用。

## 团队分工

- **卢钰：** 代码开发、产品结构、AI Native 对象系统、目标路线能力、部署与技术文档。
- **王乐：** 地图视觉、交互风格、扫帚与贴纸素材、体验叙事与展示设计。

## 黑客松展示话术

> 大多数校园地图只是把地点展示给人看。屿地图进一步把校园转化为 AI 可以理解、组合和行动的对象系统。它不仅知道美术馆在哪里，还知道它为什么值得去、适合谁、需要多少时间、附近有什么，以及能和哪些地点组成一条符合目标的路线。

## 60 秒演示脚本

1. **0–8 秒：** 展示沙漠状态开场和产品主张，点击“开始显影校园”。
2. **8–25 秒：** 选择东部区域，拖动扫帚完成显影，展示新地点出现。
3. **25–36 秒：** 点击美术馆，展示地点详情、标签和 AI 可理解对象信息。
4. **36–50 秒：** 输入“我第一次来，只有20分钟，想看最有代表性的地方”，生成路线。
5. **50–60 秒：** 展示高亮路线与推荐理由，收束到“校园不只可被看见，也可被 AI 理解”。
